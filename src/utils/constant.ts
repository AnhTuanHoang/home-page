
export const COOKIE = {
    LANG: "language",
};

export const LOCALSTORAGE = {
    LANG: "language",
};

export const FILE_FUNCTION = {
    NEWS: "news",
    BUSINESS: "business",
};

export const API_URL = `${window.location.origin}`;

export const FILE_LOCATION = {
    IMG: `${API_URL}/images`,
    MOVIE: `${API_URL}/assets/movie`,
    VIDEOS: `${API_URL}/videos`,
};